//
//  RemoteChatMessage.swift
//  play
//
//  Created by Gürhan Kuraş on 3/6/22.
//

import Foundation

/*
struct RemoteChatMessage: Identifiable, Codable {
    let id: String
    let sender: RemoteChatMessageUser
    let text: String
    let sentAt: String
    let conversationId: String
    
    var asMessage: ChatMessage {
        return ChatMessage(id: UUID().uuidString, sender: sender.name, message: text, timestamp: ISO8601DateFormatter().date(from: sentAt) ?? Date() )
    }
}

struct RemoteChatMessageUser: Codable {
    let id: String
    let name: String
    let image: String
}

*/
